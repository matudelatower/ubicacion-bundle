{% extends 'base.html.twig' %}

{% block body %}
    <h1>Provincia list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Codigo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for provincium in provincias %}
            <tr>
                <td><a href="{{ path('provincia_show', { 'id': provincium.id }) }}">{{ provincium.id }}</a></td>
                <td>{{ provincium.descripcion }}</td>
                <td>{{ provincium.codigo }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('provincia_show', { 'id': provincium.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('provincia_edit', { 'id': provincium.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('provincia_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
